#include <bits/stdc++.h>
using namespace std;

int n, Map[21][21];
vector<int> dices;

int main(){
    cin.tie(NULL); cout.tie(NULL); ios::sync_with_stdio(0);
    for(int i=0;i<10;i++){
        int x;
        cin >> x;
        dices.push_back(x);
    }
}